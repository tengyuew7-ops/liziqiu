# 数据与隐私说明

更新日期：2026-09-23

本说明适用于 Like or Love · 栗子球美食气泡网页。它说明页面在用户确认美食选择时处理哪些数据、数据用于什么目的，以及本地演示和线上模式的区别。

## 处理的数据

只有在用户点击“就吃这个”确认选择后，页面才会创建一条选择记录：

| 数据 | 来源 | 用途 |
| --- | --- | --- |
| 美食编号和名称 | 用户所选气泡 | 展示并统计选择结果 |
| 昵称 | 用户自愿填写，可留空 | 让页面维护者辨认是谁提交了选择 |
| 匿名访客编号 | 浏览器首次使用时随机生成 | 区分同一浏览器的多次提交 |
| 请求编号 | 每次确认时随机生成 | 去重和排查单次提交 |
| 客户端时间 | 用户设备 | 记录用户发起选择的时间 |
| 来源和页面版本 | 页面固定值 | 区分数据来源和格式版本 |
| 服务端创建时间 | PostgreSQL | 记录数据库收到选择的时间 |

匿名访客编号保存在浏览器 `localStorage` 的 `liziqiu-visitor-id` 键中。它不是账户，也不会核验用户身份，但在同一浏览器存储未被清除时会重复使用。

应用写入的 `food_choices` 业务表不包含密码、电话号码、电子邮箱、精确位置或 IP 地址。GitHub Pages、CloudBase、内容分发网络和网络运营商仍可能按各自服务规则处理常规请求元数据，例如 IP 地址、时间、User-Agent 和错误日志；这些平台层数据不由本仓库的业务代码写入 `food_choices` 表。

## 使用目的

上述数据仅用于：

- 把用户选择的美食展示给页面维护者；
- 在页面中确认提交结果；
- 对重复请求进行约束；
- 排查提交失败或数据格式问题。

本项目代码没有广告、用户画像或跨站跟踪功能。

## 线上模式

从公开 GitHub Pages 地址访问时，浏览器把选择通过 HTTPS 发送到 CloudBase HTTP 网关。普通云函数校验请求后，将记录写入 CloudBase PostgreSQL 的 `public.food_choices` 表。

数据库没有向匿名浏览器开放直接读取或写入权限。页面维护者可以通过 CloudBase 控制台查看记录。

## 本地演示模式

通过 `file:`、`localhost`、`127.0.0.1`、`?demo=1` 或配置项 `demoMode: true` 使用页面时，记录不会发送到 CloudBase。最近 30 条演示记录只保存在当前浏览器的 `localStorage` 键 `liziqiu-demo-choices` 中。

用户可以通过浏览器的站点数据设置清除本地记录和匿名访客编号。

## 保存期限与删除

当前 PostgreSQL 架构没有自动到期或定时删除规则，线上记录会保留，直到项目维护者手动删除。

如需查询或删除某条线上记录，请通过仓库维护者提供的联系渠道发起请求；也可以新建一个不含个人敏感信息的 GitHub Issue，请维护者说明安全的核对方式。核对时通常需要昵称、所选美食和大致提交时间。不要在公开 Issue 中提供真实姓名、联系方式、密钥或其他敏感信息。

## 第三方服务

页面使用以下第三方服务：

- **GitHub Pages**：提供静态网页；
- **腾讯云 CloudBase**：提供 HTTP 网关、云函数和 PostgreSQL；
- **jsDelivr**：加载 `canvas-confetti` 动画库。若该资源加载失败，页面会使用仓库内置的 Canvas 心形动画作为后备。

这些服务对网络请求的处理受其各自的服务条款和隐私政策约束。

## 口令说明

页面口令 `20040914` 位于公开的静态 JavaScript 中，只用于开启动画和美食选择流程。它不是安全认证，不能用于保护数据库记录或识别用户。

## 变更

如果收集字段、使用目的或后端服务发生实质变化，应同步更新本文件的内容和更新日期。
