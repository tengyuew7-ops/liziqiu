# 安全说明

## 支持范围

当前仅维护 `main` 分支的最新版本。

## 报告安全问题

请不要在公开 Issue 中发布密钥、真实访客记录或可直接利用的漏洞细节。请通过 GitHub 仓库所有者 `tengyuew7-ops` 的私密联系方式报告，并附上：

- 受影响的页面或接口；
- 复现步骤；
- 可能影响；
- 建议修复方式（如有）。

## 已知边界

网页中的 8 位数字只是互动入口码，保存在公开的前端代码中，不能用于身份认证或保护敏感信息。数据库写入由服务端云函数完成，服务端密钥不得写入浏览器代码、仓库、响应或日志。

## 依赖状态

项目当前使用腾讯云发布的最新 `@cloudbase/node-sdk` 3.18.3。该版本仍带有 npm 审计标记的旧版 `axios`、`lodash.set` 和 `lodash.unset` 间接依赖，上游暂未提供消除这些告警的兼容版本。

本云函数不接受用户提供的请求地址、代理配置或动态属性路径；进入数据库 SDK 的记录还会先经过固定字段和允许列表校验。这缩小了已知告警在本项目中的可利用范围，但不能替代上游修复。Dependabot 会持续检查新版本，升级 SDK 时必须重新运行测试并在 CloudBase 环境验证 PostgreSQL 写入。
